# PolyLife core — build & run with Docker (Windows / PowerShell).
# The React frontend is built automatically inside Docker; no local Node needed.
# After it starts, open http://localhost:8000

docker compose up --build
